name: XServer GAME 自动登录 (Playwright)

on:
  schedule:
    # 每天北京时间22:45运行 (UTC时间: 14:45)
    - cron: '45 14 * * *'
  workflow_dispatch:  # 允许手动触发

jobs:
  auto-login:
    runs-on: ubuntu-22.04
    
    # 授予工作流写入仓库内容的权限
    permissions:
      contents: write
      actions: write
    
    # 设置超时时间为20分钟 (包含邮箱验证码获取时间)
    timeout-minutes: 20
    
    steps:
    - name: 🔄 检出代码
      uses: actions/checkout@v4
      
    - name: 🐍 设置 Python 环境
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: 📦 安装 Python 依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: 🎭 安装 Playwright 浏览器
      run: |
        playwright install chromium
        
    - name: 🎌 安装日文字体支持
      run: |
        # 更新包列表
        sudo apt-get update
        
        # 安装日文字体包，支持日文显示
        sudo apt-get install -y fonts-noto-cjk fonts-noto-cjk-extra
        
        # 验证字体安装
        echo "📝 已安装的日文字体："
        fc-list | grep -i "noto.*cjk" | head -5
        
    - name: 🚀 运行 XServer 完全自动化登录
      env:
        # XServer 登录凭据 (main.py自动读取)
        XSERVER_EMAIL: ${{ secrets.XSERVER_EMAIL }}
        XSERVER_PASSWORD: ${{ secrets.XSERVER_PASSWORD }}
        
        # 邮箱验证码获取凭据 (main.py内置邮箱功能自动读取)
        WEBMAIL_USERNAME: ${{ secrets.WEBMAIL_USERNAME }}
        WEBMAIL_PASSWORD: ${{ secrets.WEBMAIL_PASSWORD }}
        
        
        # 以下变量由系统和脚本自动处理：
        # - GITHUB_ACTIONS: GitHub自动设置为"true"
        # - USE_HEADLESS: main.py检测到GITHUB_ACTIONS时自动启用无头模式
        # - VERIFICATION_TIMEOUT: main.py默认180秒超时
      run: |
        echo "🚀 启动XServer GAME完全自动化登录流程..."
        echo "📧 XServer邮箱: $XSERVER_EMAIL"
        echo "📮 验证邮箱: $WEBMAIL_USERNAME@zmkk.edu.kg"
        echo "🤖 运行环境: GitHub Actions (自动无头模式)"
        echo "⏰ 验证码获取超时: 180秒"
        echo "📂 工作目录: $(pwd)"
        echo "🐍 Python版本: $(python --version)"
        echo "🎭 浏览器引擎: Playwright (Chromium)"
        echo ""
        
        # 检查主脚本文件
        echo "📋 检查主脚本文件："
        ls -la main.py
        echo ""
        
        # 运行单一主脚本，包含完整的登录和验证码获取功能
        # 基于Playwright + stealth 实现完全自动化
        python main.py
        
    - name: 📸 上传运行结果
      if: always()  # 无论成功失败都上传
      uses: actions/upload-artifact@v4
      with:
        name: xserver-auto-login-results-${{ github.run_number }}
        path: |
          *.png
        retention-days: 7  # 保留7天
        
    - name: 🧹 清理旧的工作流运行记录
      uses: MajorScruffy/delete-old-workflow-runs@v0.3.0
      env:
        GITHUB_TOKEN: ${{ github.token }}
      with:
        repository: ${{ github.repository }}
        older-than-seconds: 172800  # 删除2天前的记录
